using System;
using System.Windows;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace PianoTutorial.Common
{
    public class BindViewModel2TheView : DependencyObject 
    {
        //public static readonly DependencyProperty ViewModelKeyProperty = DependencyProperty.RegisterAttached(("ViewModelKey"), typeof(string), typeof(BindViewModel2TheView), (PropertyMetadata ,new FrameworkMetaData((object)string.Empty, new PropertyChangedCallback(BindViewModel2TheView.ViewModelKeyChange))));
    }
}
